Duck Habit Hub v24.84 — Crown + Installed App Update Fix

- Raises Io's crown slightly from the v24.83 placement.
- Moves the Hub to a stable sw.js service-worker URL.
- Adds version.json build checks with no-store network requests.
- Installed PWA checks for a newer deployed build on load, pageshow, and whenever it resumes from the background.
- New service worker claims clients immediately and removes older Duck Habit Hub caches.
- Cache-reset page now reminds Android users to close an already-open PWA from Recents once after this migration.

Save data is unchanged.

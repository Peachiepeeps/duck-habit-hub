Duckie Days v24.174 — Verified Recovery

Why this build exists
The previous v24.173 workflow could complete green even when the update ZIP was not found. In that case all actual install/verify/cleanup steps were skipped, so the app did not change.

This v24.174 installer is fail-loud:
- It requires the exact update ZIP to be present at the repository root.
- If the ZIP is missing, the workflow exits RED and prints the root file listing.
- If the ZIP is present, it installs and verifies all version references before cleanup.
- It uses new v24.174 filenames/cache names to force a fresh PWA build.
- It cleans old Duckie Days installer workflows only after a successful verification.

Expected active files after success
- style-v24-174.css
- script-v24-174.js
- sw-v24-174.js
- duck-quest/css/style-v77.css
- duck-quest/js/game-v89.js
- version.json = 24.174

Duckie Days v24.179 — External Hatchery Recovery

This update leaves the now-correct home buttons alone.

Hatching Area change:
- Adds a separate hatchery-v179.js loaded after the main Duck Quest file.
- Removes old hatchery surfaces (#hatcheryScreen and #hatcheryScreenV178).
- Clones the Hatching Area home button to remove all legacy click handlers.
- Opens the new hatchery directly instead of calling the legacy showScreen("hatchery") chain.
- Embeds Hatching-background-new.png directly in the recovery script.
- Reuses the existing egg inventory/incubator/hatching API and save data.
- Uses the normal top-left Duck Quest back button to return home.

This specifically addresses the symptom where only the old brown top border and blue/white gradient appeared.
